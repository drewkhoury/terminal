# Setup 
 
```
sh ./config_setup.sh

# check for folders with 777 permissions
compaudit

# fix 777 permissions
sudo chmod -R 755 /usr/local/share/zsh

# fix issue with .zsh? (remove all of it if it gives you issues)
vim .zsh.after/nvm.zsh

brew --cask install font-source-code-pro

vim ~/.gitconfig.user

[user]
	email = drew.khoury@gmail.com
	name = Andrew Khoury

```
 
# Linux Tips

```
# vim
nt
shift u

# tmux
...

# config (for this repos config)
config diff

```
